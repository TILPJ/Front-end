import styled from 'styled-components';

const StyledSectionWrapper = styled.div`
  width: 100%;
  height: 529px;
`;

const FirstSection = () => {
  return <StyledSectionWrapper>test</StyledSectionWrapper>;
};
export default FirstSection;
